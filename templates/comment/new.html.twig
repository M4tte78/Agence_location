{% extends 'base.html.twig' %}

{% block title %}Ajouter un commentaire{% endblock %}

{% block body %}
<h1 class="text-center mb-4">Ajouter un commentaire</h1>

<div class="card shadow-lg p-4">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="text-center mt-3">
            <button type="submit" class="btn btn-primary">Publier</button>
        </div>
    {{ form_end(form) }}
</div>

<div class="text-center mt-3">
    <a href="{{ path('app_vehicle_show', { id: vehicle.id }) }}" class="btn btn-secondary">Retour</a>
</div>
{% endblock %}
